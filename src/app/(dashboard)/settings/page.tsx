import { EmptyState } from "@/components/dashboard/EmptyState";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Settings and account management placeholders are ready for future auth and environment config."
    />
  );
}

